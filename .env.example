# CarryWorks — nothing is deployed yet.
#
# Any address left unset renders as "Awaiting launch" on the site, and
# `isLive` stays false, which gates every claim the pages make about being
# live. Fill these in only once the contracts actually exist.

NEXT_PUBLIC_CARRYWORKS_FACTORY=
NEXT_PUBLIC_CARRYWORKS_LENS=
NEXT_PUBLIC_CARRYWORKS_FEE_COLLECTOR=
